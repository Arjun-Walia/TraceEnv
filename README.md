# TraceEnv
TraceEnv is a local-first workspace synthesizer that automatically generates reproducible setup documentation by observing real terminal workflows. TraceEnv passively watches successful commands in your shell and reconstructs the exact steps required to recreate the development environment. 
